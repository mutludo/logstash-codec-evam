input {
    file {
        path => "/Users/mertnuhoglu/projects/study/study/logstash/plain.log"
        start_position => beginning 
				codec => plain{
				}
    }
}
output {
	elasticsearch {
		protocol => "http"
		host => localhost
		index => plain
	}
	stdout {}
}

